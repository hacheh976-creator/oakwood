// lib/screens/admin/storefront/admin_categories_screen.dart
import 'package:flutter/material.dart';
import 'package:provider/provider.dart';

import '../../../providers/product_provider.dart';
import '../../../models/category.dart';

class AdminCategoriesScreen extends StatelessWidget {
  const AdminCategoriesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    const brown = Color(0xFF5C3B1E);
    const background = Color(0xFFF5EDE3);

    final pp = context.watch<ProductProvider>();
    final categories = pp.categories;

    return Scaffold(
      backgroundColor: background,
      appBar: AppBar(
        backgroundColor: brown,
        title: const Text('Categories'),
      ),
      floatingActionButton: FloatingActionButton(
        backgroundColor: brown,
        onPressed: () {
          // Later: open add-category dialog
        },
        child: const Icon(Icons.add),
      ),
      body: ListView.builder(
        padding: const EdgeInsets.all(16),
        itemCount: categories.length,
        itemBuilder: (context, index) {
          final CategoryModel c = categories[index];

          return Card(
            shape: RoundedRectangleBorder(
              borderRadius: BorderRadius.circular(16),
            ),
            margin: const EdgeInsets.only(bottom: 12),
            child: ListTile(
              leading: ClipRRect(
                borderRadius: BorderRadius.circular(10),
                child: Image.network(
                  c.imageUrl,
                  width: 60,
                  height: 60,
                  fit: BoxFit.cover,
                ),
              ),
              title: Text(c.name),
              trailing: Row(
                mainAxisSize: MainAxisSize.min,
                children: [
                  IconButton(
                    icon: const Icon(Icons.edit),
                    onPressed: () {
                      // Later: edit category
                    },
                  ),
                  IconButton(
                    icon: const Icon(Icons.delete_outline),
                    onPressed: () {
                      pp.deleteCategory(c.id);
                    },
                  ),
                ],
              ),
            ),
          );
        },
      ),
    );
  }
}