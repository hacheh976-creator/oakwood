import 'package:flutter/material.dart';

import 'business_report_screen.dart';
import 'sales_management_screen.dart';
import 'inventory_screen.dart';
import 'expenses_screen.dart';
import 'attendance_screen.dart';
import 'admin_settings_screen.dart';
import 'order_management_screen.dart';
import 'admin_storefront_screen.dart'; // ✅ NEW – edit user side (Storefront)

class AdminMainScreen extends StatelessWidget {
  const AdminMainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xFFD4C4B0),
      appBar: AppBar(
        backgroundColor: const Color(0xFFF5EFE7),
        foregroundColor: const Color(0xFF4A3A2A),
        elevation: 0,
        centerTitle: true,
        title: const Text(
          'Admin Panel',
          style: TextStyle(fontWeight: FontWeight.w900),
        ),
      ),
      body: const _AdminHomeGrid(),
    );
  }
}

class _AdminHomeGrid extends StatelessWidget {
  const _AdminHomeGrid();

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(14),
      child: GridView.count(
        crossAxisCount: 2,
        crossAxisSpacing: 12,
        mainAxisSpacing: 12,
        childAspectRatio: 1.15,
        children: [
          _AdminTile(
            title: "Business Report",
            subtitle: "Summary & Stats",
            icon: Icons.bar_chart_rounded,
            onTap: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (_) => const BusinessReportScreen(),
                ),
              );
            },
          ),

          _AdminTile(
            title: "Sales",
            subtitle: "Orders & Bills",
            icon: Icons.point_of_sale_rounded,
            onTap: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (_) => const SalesManagementScreen(),
                ),
              );
            },
          ),

          _AdminTile(
            title: "Order Management",
            subtitle: "Customer Orders & Agreements",
            icon: Icons.assignment_rounded,
            onTap: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (_) => const OrderManagementScreen(),
                ),
              );
            },
          ),

          _AdminTile(
            title: "Inventory",
            subtitle: "Products & Stock",
            icon: Icons.inventory_2_rounded,
            onTap: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (_) => const InventoryScreen(),
                ),
              );
            },
          ),

          _AdminTile(
            title: "Expenses",
            subtitle: "Costs & Payments",
            icon: Icons.receipt_long_rounded,
            onTap: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (_) => const ExpensesScreen(),
                ),
              );
            },
          ),

          _AdminTile(
            title: "Attendance",
            subtitle: "Staff Records",
            icon: Icons.people_alt_rounded,
            onTap: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (_) => const AttendanceScreen(),
                ),
              );
            },
          ),

          // 🆕 NEW: Storefront editor – controls user-side app (home, categories, products)
          _AdminTile(
            title: "Storefront",
            subtitle: "Edit User App UI",
            icon: Icons.store_mall_directory_rounded,
            onTap: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (_) => const AdminStorefrontScreen(),
                ),
              );
            },
          ),

          _AdminTile(
            title: "Settings",
            subtitle: "Theme / Logout",
            icon: Icons.settings_rounded,
            onTap: () {
              Navigator.push(
                context,
                MaterialPageRoute(
                  builder: (_) => const AdminSettingsScreen(),
                ),
              );
            },
          ),
        ],
      ),
    );
  }
}

class _AdminTile extends StatelessWidget {
  const _AdminTile({
    required this.title,
    required this.subtitle,
    required this.icon,
    required this.onTap,
  });

  final String title;
  final String subtitle;
  final IconData icon;
  final VoidCallback onTap;

  @override
  Widget build(BuildContext context) {
    return Material(
      color: const Color(0xFFF5EFE7),
      borderRadius: BorderRadius.circular(18),
      elevation: 2,
      child: InkWell(
        borderRadius: BorderRadius.circular(18),
        onTap: onTap,
        child: Padding(
          padding: const EdgeInsets.all(14),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Container(
                height: 46,
                width: 46,
                decoration: BoxDecoration(
                  borderRadius: BorderRadius.circular(14),
                  color: const Color(0xFF4A3A2A).withAlpha(30),
                ),
                child: Icon(
                  icon,
                  size: 24,
                  color: const Color(0xFF4A3A2A),
                ),
              ),
              const SizedBox(height: 12),
              Text(
                title,
                style: const TextStyle(
                  fontSize: 16,
                  fontWeight: FontWeight.w900,
                  color: Color(0xFF4A3A2A),
                ),
              ),
              const SizedBox(height: 6),
              Text(
                subtitle,
                style: TextStyle(
                  fontSize: 12,
                  fontWeight: FontWeight.w600,
                  color: const Color(0xFF4A3A2A).withAlpha(180),
                ),
              ),
              const Spacer(),
              const Row(
                children: [
                  Text(
                    "Open",
                    style: TextStyle(
                      fontWeight: FontWeight.w900,
                      color: Color(0xFF4A3A2A),
                    ),
                  ),
                  SizedBox(width: 6),
                  Icon(
                    Icons.arrow_forward_rounded,
                    size: 18,
                    color: Color(0xFF4A3A2A),
                  ),
                ],
              ),
            ],
          ),
        ),
      ),
    );
  }
}